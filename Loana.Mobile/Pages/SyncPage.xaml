<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:Loana.Mobile.PageModels"
             xmlns:models="clr-namespace:Loana.Mobile.Models"
             xmlns:controls="clr-namespace:Loana.Mobile.Pages.Controls"
             xmlns:pullToRefresh="clr-namespace:Syncfusion.Maui.Toolkit.PullToRefresh;assembly=Syncfusion.Maui.Toolkit"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="Loana.Mobile.Pages.SyncPage"
             x:DataType="pageModels:SyncPageModel"
             x:Name="OverviewPage"
             Title="Sync">
    <VerticalStackLayout>
        <Grid RowDefinitions="*,*">
            <Entry Text="{Binding Address}" Grid.Row="0" HeightRequest="20" />
            <Button Text="Sync" Command="{Binding SyncCommand}" Grid.Row="1" />
        </Grid>
    </VerticalStackLayout>
</ContentPage>